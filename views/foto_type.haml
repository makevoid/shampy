%h1
  Foto:
  %span= @type.capitalize


.photo_gallery
  - @photos = @photos.shuffle
  - size = @photos.size
  - @photos[0..size].each_with_index do |photo, idx|
    .box{ class: "pos#{size-idx}" }
      %img{ src: photo.path }