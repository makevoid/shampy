.user
  %h1= @user.username

  - if me?
    %nav.inpage
      - link_to "Edit profile", "/users/#{@user.id}/edit", class: "btn"

  .role
    = @user.role

  -# .avatar
  -#   = @user.avatar_image

  %div

    -# .signature
    -#   = markup @user.signature


    - if File.exist? "#{Shampy.root}/views/_ext_user.haml"
      = partial :ext_user

    .clear